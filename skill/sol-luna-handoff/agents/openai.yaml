interface:
  display_name: "Sol → Luna Handoff"
  short_description: "Use adaptive routing for the lowest-cost suitable tier."
  default_prompt: "Use $sol-luna-handoff to select the lowest-cost suitable tier and complete this task with its adaptive route."
