interface:
  display_name: "Sol → Terra/Luna Handoff"
  short_description: "Default Sol planning with Luna execution; adaptive is explicit."
  default_prompt: "Use $sol-luna-handoff with Sol-to-Luna as the default execution profile. Read any persisted profile, choose conditional Luna discovery and the minimum necessary Sol planning, and use adaptive Terra/Luna routing only when adaptive was explicitly selected."
